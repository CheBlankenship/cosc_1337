// When you are creating a variable, you need to know what type of data
// the program will be storing in it.
#include <iostream>

int main() {

    // Example of variable definition.
    // 'double' uses 64-bit to calculate the value(more accurate but more memory).
    double pay, rate, hours;
    
    return 0;
}
